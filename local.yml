---
- hosts: localhost
  vars:
      URL_GIT: https://github.com/robsonvitor/ansible-meu-desktop.git
      sha256sum_ansible_pull: "300f5455fe8ca350a81f8202da317b7f77a8a65515f930bcae4b7d56adb192e7"
  pre_tasks:
    - name: Atualizando repositórios e pacotes instalados
      apt:
        update_cache: yes
        upgrade: safe
      changed_when: False

  tasks:
    - include: tasks/packages.yml
    - include: tasks/crontab.yml
